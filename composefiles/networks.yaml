networks:
  frontend:
    name: frontend
    ipam:
      config:
        - subnet: $FRONTEND_SUBNET
          gateway: $FRONTEND
  backend:
    name: backend
    ipam:
      config:
        - subnet: $BACKEND_SUBNET
          gateway: $BACKEND